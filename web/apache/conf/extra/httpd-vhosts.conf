# Virtual Hosts
#
# Required modules: mod_log_config

# If you want to maintain multiple domains/hostnames on your
# machine you can setup VirtualHost containers for them. Most configurations
# use only name-based virtual hosts so the server doesn't need to worry about
# IP addresses. This is indicated by the asterisks in the directives below.
#
# Please see the documentation at 
# <URL:http://httpd.apache.org/docs/2.4/vhosts/>
# for further details before you try to setup virtual hosts.
#
# You may use the command line option '-S' to verify your virtual host
# configuration.

Define HOMEROOT "C:\Users\Administrator\Documents\GitHub\TeraOnlineServer_Shock_1732\web\htdocs"

#
# Use name-based virtual hosting.
#
NameVirtualHost *:80
#
# VirtualHost example:
# Almost any Apache directive may go into a VirtualHost container.
# The first VirtualHost section is used for all requests that do not
# match a ##ServerName or ##ServerAlias in any <VirtualHost> block.
#

# Dyscovery
<VirtualHost 51.210.41.122>
DocumentRoot "${HOMEROOT}\dyscoverydev\public"
ServerName dyscovery.tk
ServerAlias www.dyscovery.tk

SSLEngine off
SSLCertificateFile "C:\Certbot\live\dyscovery.tk\cert.pem"
SSLCertificateKeyFile "C:\Certbot\live\dyscovery.tk\privkey.pem"
SSLCertificateChainFile "C:\Certbot\live\dyscovery.tk\fullchain.pem"

<Directory "${HOMEROOT}\dyscoverydev\public">
    Options Indexes FollowSymLinks Includes ExecCGI
    AllowOverride All
    Require all granted
	Order Allow,Deny
    Allow from all
</Directory>
</VirtualHost>

# terawip

<VirtualHost 51.210.41.122>
DocumentRoot "${HOMEROOT}\tera-web"
ServerName terawip.tk
ServerAlias www.terawip.tk

SSLEngine off
SSLCertificateFile "C:\Certbot\live\terawip.tk\cert.pem"
SSLCertificateKeyFile "C:\Certbot\live\terawip.tk\privkey.pem"
SSLCertificateChainFile "C:\Certbot\live\terawip.tk\fullchain.pem"

<Directory "${HOMEROOT}\tera-web">
    Options Indexes FollowSymLinks Includes ExecCGI
    AllowOverride None
    Require all granted
	Order Allow,Deny
    Allow from all
</Directory>
</VirtualHost>

# building.terawip

<VirtualHost 51.210.41.122>
DocumentRoot "${HOMEROOT}\wordpress"
ServerName building.terawip.tk
ServerAlias www.building.terawip.tk
<Directory "${HOMEROOT}\wordpress">
    AllowOverride All
    Options Indexes FollowSymLinks Includes ExecCGI
    Require all granted
	Order Allow,Deny
    Allow from all
</Directory>
</VirtualHost>